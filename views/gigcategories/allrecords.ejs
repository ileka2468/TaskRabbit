<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head') %>
    <title>Document</title>
</head>
<body>
<%- include('../partials/header') %>

<main>

    <% if (allrecs.length > 0) {%>
        <table border="1">
            <tr>
                <th>Gig Category ID</th>
                <th>Sub Category ID</th>
                <th>Gig Category Name</th>
            </tr>
            <% allrecs.forEach((recref, index) => { %>
                <tr>
                    <td><%= recref.gig_category_id %></td>
                    <td><%= recref.subcategory_id %></td>
                    <td><%= recref.gig_category %></td>
                    <td><a href='/gigcategories/<%= recref.gig_category_id %>/show'>
                            Details</a></td>
                    <td><a href='/gigcategories/<%= recref.gig_category_id %>/edit'>Edit</a></td>
                    <td><a href='/gigcategories/<%= recref.gig_category_id %>/delete'
                           onclick="return confirm('Are you sure you want to delete this record?');">Delete</a></td>
                </tr>
            <% }); %>
        </table>
    <% } else { %>
        <p>Not Records Available</p>
    <% } %>
    <p> <a href='/gigcategories/addrecord'>Add New</a> </p>
</main>

<%- include('../partials/footer') %>
</body>
</html>