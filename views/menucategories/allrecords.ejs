<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('../partials/head') %>
    <title>Document</title>
</head>
<body>
<%- include('../partials/header') %>

<main>

    <% if (allrecs.length > 0) {%>
        <table border="1">
            <tr>
                <th>Menu Category ID</th>
                <th>Men Category Name</th>
            </tr>
            <% allrecs.forEach((recref, index) => { %>
                <tr>
                    <td><%= recref.menucategory_id %></td>
                    <td><%= recref.cat_name %></td>
                    <td><a href='/menucategories/<%= recref.menucategory_id %>/show'>
                            Details</a></td>
                    <td><a href='/menucategories/<%= recref.menucategory_id %>/edit'>Edit</a></td>
                    <td><a href='/menucategories/<%= recref.menucategory_id %>/delete'
                           onclick="return confirm('Are you sure you want to delete this record?');">Delete</a></td>
                </tr>
            <% }); %>
        </table>
    <% } else { %>
        <p>Not Records Available</p>
    <% } %>
    <p> <a href='/menucategories/addrecord'>Add New</a> </p>
</main>

<%- include('../partials/footer') %>
</body>
</html>